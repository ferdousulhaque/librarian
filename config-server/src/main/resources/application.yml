spring:
  bus:
    enabled: true
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/ferdousulhaque/librarian/tree/main/configs"
server:
  port: 8888